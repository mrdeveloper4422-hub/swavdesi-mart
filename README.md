# Swavdesi Mart

A lightweight storefront demo with a keyboard-protected admin panel.

## Run locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

## Admin panel access

1. Press `Ctrl + 1`.
2. Enter admin password: `admin@123`.
3. Add products from the admin panel form.
